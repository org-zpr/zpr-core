[global]
name = "web"
ca_file = "auth-ca.crt"
certificate_file = "adapter-web-noise.crt"
private_key_file = "adapter-web-noise.key"
zpr_addr = [ "fd5a:5052:1::8" ] 
self_addr = "0.0.0.0:5000"

# use `sudo ./mktun.sh` to set this up
tun_if = "tun9"


[adapter]
# TODO: Can we use the OCI private/internal addr here?
node_addr = "10.0.0.21:5000"
node_public_key_file = "node-noise-pub.pem"









