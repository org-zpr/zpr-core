name: Rust-cslab

on:
  push:
    branches: [ "main" ]
    paths:
      - 'cslab/**'
      - '.github/workflows/cslab.yml'
  pull_request:
    paths:
      - 'cslab/**'
      - '.github/workflows/cslab.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  cslab-build:
    name: Cslab
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-build-test.yml@18db4a3e7ba9852e3cd4a330843742bd93406c48
    with:
      name: 'Cslab'
      path: './cslab'
    secrets: inherit
