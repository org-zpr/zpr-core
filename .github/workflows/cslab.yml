name: Rust-cslab

on:
  push:
    branches: [ "main" ]
    paths:
      - 'cslab/**'
      - '.github/workflows/cslab.yml'
  pull_request:
    paths:
      - 'cslab/**'
      - '.github/workflows/cslab.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  cslab-build:
    name: Cslab
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-build-test.yml@4cda3cd92373d0df4e800a02c528020ee6291170
    with:
      name: 'Cslab'
      path: './cslab'
    secrets: inherit
