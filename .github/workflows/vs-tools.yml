name: Rust-visaservice

on:
  push:
    branches: [ "main" ]
    paths:
      - 'libnode/**'
      - 'vs-client/**'
      - '.github/workflows/vs-tools.yml'
  pull_request:
    paths:
      - 'libnode/**'
      - 'vs-client/**'
      - '.github/workflows/vs-tools.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  build-vs:
    name: ${{ matrix.target }}
    strategy:
      matrix:
        target: [vs-client]
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-containerized-build-test.yml@36addd896020351f374499592ba0afbda3fb6ea4
    with:
      name: ${{ matrix.target }}
      path: ./${{ matrix.target }}
    secrets: inherit
