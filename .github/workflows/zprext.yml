name: Rust-zprext

on:
  push:
    branches: [ "main" ]
    paths:
      - 'zpr-ext/**'
      - '.github/workflows/zprext.yml'
  pull_request:
    paths:
      - 'zpr-ext/**'
      - '.github/workflows/zprext.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  zpr-ext:
    name: ZPR Ext
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-build-test.yml@8695c803f1b0ec057ea60a1379eb63d7241a5c9a
    with:
      name: 'ZPR-Ext'
      path: './zpr-ext'
    secrets: inherit
