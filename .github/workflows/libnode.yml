name: Rust-node

on:
  push:
    branches: [ "main" ]
    paths:
      - 'libnode/**'
      - '.github/workflows/libnode.yml'
  pull_request:
    paths:
      - 'libnode/**'
      - '.github/workflows/libnode.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  libnode-build:
    name: Libnode
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-containerized-build-test.yml@4cda3cd92373d0df4e800a02c528020ee6291170
    with:
      name: Libnode
      path: ./libnode
    secrets: inherit
