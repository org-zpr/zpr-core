name: Rust-node

on:
  push:
    branches: [ "main" ]
    paths:
      - 'libnode/**'
      - '.github/workflows/libnode.yml'
  pull_request:
    paths:
      - 'libnode/**'
      - '.github/workflows/libnode.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  libnode-build:
    name: Libnode
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-containerized-build-test.yml@b51011c7c57ecf8804e0a031562523468327d7c3
    with:
      name: Libnode
      path: ./libnode
    secrets: inherit
