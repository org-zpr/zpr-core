name: Rust-visaservice

on:
  push
  pull_request

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build
      run: cd visaservice && make all-rs

    - name: Test
      run: cd visaservice && make test-rs

